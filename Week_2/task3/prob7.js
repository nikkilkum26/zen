var arr = [1, 2, 3];
function hourToSeconds(num) {
	for (let i=0; i<num.length;i++){
	    
		returnFunction(num[i]*3600);
	}
    
    
}
function returnFunction(n){
    console.log(n);
}
var data = hourToSeconds(arr);
